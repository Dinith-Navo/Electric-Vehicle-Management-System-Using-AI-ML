# Saved Models: Queue Prediction
Place Google Colab exported model files here:
- `.joblib` (recommended for Scikit-Learn models like RandomForestRegressor, GradientBoosting, TimeSeries/K-Means)
- `.pkl` or `.pickle`

When a file is placed here, the `QueueModelLoader` will automatically detect and load it on next service startup without any code modifications.
